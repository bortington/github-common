on:
  workflow_call:
    inputs:
      username:
        required: true
        type: string
        
jobs:
  blah:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        echo "Hello ${{inputs.username}}"
